using SemanticMachine;

Console.WriteLine("Hello, World!");

//string input = "a.add(b, c, bar.foo()).add(e, a + b)";

//IEnumerable<string> tokens = Tokenizer.ProtoTokenize(input);

//foreach (string token in tokens.SelectMany<string, string>((x, i) => i == 0 ?[x] : [" ", x]))
//{
//    Console.Write(token);
//}
//Console.WriteLine();

//foreach (string segment in LispSynthesizer.PrettyPrint(LispSynthesizer.Synthesize(tokens.ToList())))
//{
//    Console.Write(segment);
//}

// test the parser


